require 'spec_helper'

describe WebpackHelperRails do
  it 'has a version number' do
    expect(WebpackHelperRails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
